name: medusa-platform
runtime:
  name: nodejs
  options:
    packagemanager: yarn
description: >-
  Organization-level infrastructure: the folder hierarchy, one project per app per
  environment, and the identities that let app repositories deploy into them.
